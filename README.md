# Azure Data Pipeline with Terraform

This project deploys a **data pipeline** in **Microsoft Azure** using **Terraform**.  
It stores raw data in **Azure Blob Storage** and processes it using an **Azure Function**.

## 📌 Features:
✅ **Creates an Azure Storage Account** for storing raw & processed data.  
✅ **Deploys an Azure Function** to process data.  
✅ **Implements Infrastructure as Code (IaC)** with Terraform.

## 🚀 Deployment Steps:
1. Install **Terraform** and **Azure CLI**.
2. Run `terraform init` to initialize.
3. Run `terraform apply -auto-approve` to deploy resources.

## 🔗 Author:
John Jose - [GitHub](https://github.com/johnnereparambil)
